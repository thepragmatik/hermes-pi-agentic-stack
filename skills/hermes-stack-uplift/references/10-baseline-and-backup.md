# Phase 10 — Baseline and Backup

Snapshot current Hermes/Pi installs and configuration. Preserve the previous Hermes context DB read-only; export only reviewed durable knowledge rather than bulk-importing history. Capture representative mission telemetry: prompt/input/output/cache tokens, TTFT, retries, tool errors, accepted-task success, human intervention and workstation memory pressure. Create a rollback tag/checkpoint.

Evidence: backup location, checksums, baseline metrics, rollback rehearsal.
