# Phase 60 — Evaluation and Multi-Role Promotion

Run the complete matched/adversarial corpus across context/skills + LCM/Mnemosyne, router shadow, OpenRouter role/model/provider policy, external security/egress, Pi RPC/containment/retry, LSP language fixtures, Spec Kit profiles, bounded task/review flow and rollback.

Compare to Phase 10 on:

- accepted-task quality/success;
- total/fresh/cached tokens and cache-read share;
- TTFT/wall time/throughput;
- route-switch and physical-provider continuity;
- retries/tool errors/human intervention;
- durable-memory relevance/staleness;
- workstation RSS/memory pressure/swap;
- security/privacy violations;
- cost and minutes per accepted task.

Role/model IDs remain runtime bindings. Benchmark current OpenRouter candidates on frozen tasks; research snapshot intent is GLM-5.3-Flash-class coding/bootstrap and DeepSeek-V4-Flash-class research. Bind `review.default` to an independent family only after evidence. Optional cheap auxiliary must earn its complexity.

Periodically benchmark direct Z.ai/DeepSeek endpoints against OpenRouter, but direct access is an exception: it receives credentials only if the material improvement in accepted-task economics/reliability/privacy/rate limits justifies another integration surface.

Any mandatory privacy/capability failure or material accepted-task quality regression blocks promotion.

## Promotion Checkpoint E

Only after the complete system-level promotion gate passes may ordinary operation become:

```text
local deterministic policy
 -> local mission router with authority
 -> role/model binding
 -> OpenRouter
 -> policy-compatible physical provider
```

OpenRouter Auto remains bounded to explicitly approved fallback/auxiliary cases.

If policy requires human approval for production promotion, request it **only after every mandatory P0/security/quality/economic gate for that promotion has passed**. Approval is additional authority and cannot waive, replace or override failed/missing mandatory evidence.

After evidence-qualified approval, start a fresh ordinary Hermes session on the final promoted router/model/provider/context configuration. Record exact Hermes/Pi/LCM/Mnemosyne/router/model/provider-policy pins and rollback checkpoint.

Persist state/evidence, send the required phase-boundary report, and stop before Phase 70.
