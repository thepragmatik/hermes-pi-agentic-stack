# Phase 40 — Security and Policy

Compile policy intent into real sandbox, filesystem, process, credential, network/egress and merge controls. Add deterministic secret detection plus PII detection/redaction/tokenization. Seed canaries and prove local-only/secret payloads fail closed. Strip environment/credentials from workers; broker only scoped credentials when required.

Do not promote while policy exists only as YAML/prompts.
